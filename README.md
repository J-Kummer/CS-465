module2 adds a new folder named app_server. 
It also adjust the Handlebars registers the new folders and changes to app.js
app_server holds the controller, routes and views.
views holds a new partials folder that the <div> header and footer 
under header.hbs and footer.hbs
views holds a sub-folder named layouts holding the layout.hbs.


